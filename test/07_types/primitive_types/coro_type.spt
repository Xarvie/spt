// 测试: 类型系统 - coro协程类型
// 语法: primitiveType -> COROUTINE
// 描述: 测试coro类型声明

coro c = null;
assert(typeof(c) == "null", "typeof(null) should be 'null'");

coro co;
assert(typeof(co) == "null", "typeof(uninitialized coro) should be 'null'");

print("coro_type tests passed!");
