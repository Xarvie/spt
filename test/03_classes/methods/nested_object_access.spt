// 测试: 类 - 嵌套对象访问
// 语法: postfixMemberSuffix
// 描述: 测试嵌套对象的成员访问

class Inner {
    int value;
    void __init(int v) {
        this.value = v;
    }
}

class Outer {
    any inner;
    void __init(int v) {
        this.inner = new Inner(v);
    }
}

Outer o = new Outer(42);
print(o.inner.value);
