// 测试: 类 - A.B 不带括号
// 语法: postfixCallSuffix
// 描述: 测试直接调用命名空间中的类名不带括号会返回类本身

class Inner {
    int value = 100;
}

map<any, any> NS = {};
NS.Inner = Inner;

auto obj = NS.Inner;
assert(typeof(obj) == "table", "obj should be the class itself");

print("qualified_no_parens tests passed!");
