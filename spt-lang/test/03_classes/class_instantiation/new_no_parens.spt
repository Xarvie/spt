// 测试: 类 - 直接调用不带括号
// 语法: postfixCallSuffix
// 描述: 测试直接调用类名不带括号会返回类本身（不是实例）

class Point {
    int x = 0;
    int y = 0;
}

auto p = Point;
assert(typeof(p) == "table", "p should be the class itself");

print("no_parens tests passed!");
