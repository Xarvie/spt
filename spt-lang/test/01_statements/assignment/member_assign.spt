// 测试: 语句 - 成员赋值
// 语法: lvalueSuffix -> DOT IDENTIFIER #lvalueMember
// 描述: 测试对象成员作为左值的赋值

class Point {
    int x;
    int y;
}

Point p = Point();
p.x = 10;
p.y = 20;
assert(p.x == 10, "p.x should be 10");
assert(p.y == 20, "p.y should be 20");

print("member_assign tests passed!");
