// 验证 B5: lua_movearray 缺 GC 写屏障
// 这个 bug 在增量 GC 下可能触发 use-after-free
// 很难直接触发，但可以尝试强制 GC 来观察

list<int> dst = [0, 0, 0];
list<int> src = [100, 200, 300];

// 尝试触发 lua_movearray 路径（通过 list.move 或类似 API）
// SPT 可能没有直接暴露 lua_movearray，跳过直接测试
print("B5: lua_movearray is C-API only, cannot test from .spt directly");
print("B5: SKIPPED - requires C API test harness");
