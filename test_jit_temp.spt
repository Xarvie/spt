// JIT stress test: hot loop with integer arithmetic

int sum = 0;
for (int i = 0, 1000000) {
    sum = sum + i;
}
print(sum);
assert(sum == 500000500000, "sum mismatch");
print("JIT stress test passed!");
